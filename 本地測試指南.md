# 🧪 本地測試指南

## ✅ 準備工作已完成

我已經為你創建了 `start-dev.bat` 啟動腳本。

---

## 🚀 啟動應用程式（手動執行）

### 方法 1：使用啟動腳本（推薦）

1. **雙擊執行**：
   ```
   C:\my-booking-app-practice\start-dev.bat
   ```

2. **等待啟動**：
   - 看到 Maven 下載依賴
   - 看到 Spring Boot 啟動日誌
   - 看到 "Started BookingApplication in X seconds"

3. **測試應用**：
   - 打開瀏覽器
   - 訪問：http://localhost:8080

### 方法 2：使用 PowerShell（手動）

打開 PowerShell，執行：

```powershell
# 設定環境變數
$env:DATABASE_URL="jdbc:mysql://localhost:3306/booking_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Taipei"
$env:DB_USERNAME="root"
$env:DB_PASSWORD="2FTA93108"
$env:MAIL_USERNAME="howie960018@gmail.com"
$env:MAIL_PASSWORD="rgsqpqcanthwqars"
$env:APP_BASE_URL="http://localhost:8080"

# 切換目錄
cd C:\my-booking-app-practice\booking

# 啟動應用
mvn spring-boot:run
```

---

## 📋 啟動檢查清單

### 啟動前檢查

- [ ] **MySQL 服務正在運行**
  ```cmd
  tasklist | findstr mysql
  ```
  應該看到 `mysqld.exe` 進程 ✅（已確認運行中）

- [ ] **資料庫 booking_db 存在**
  - 如果不存在，需要創建：
  ```sql
  CREATE DATABASE booking_db;
  ```

- [ ] **環境變數已設定**
  - 使用 `start-dev.bat` 會自動設定 ✅

### 啟動後檢查

- [ ] **應用程式成功啟動**
  - 看到：`Started BookingApplication in X.XXX seconds`
  - 沒有紅色 ERROR 訊息

- [ ] **資料表自動創建**
  - `spring.jpa.hibernate.ddl-auto=create-drop` 會自動創建資料表

- [ ] **測試數據自動載入**
  - `data.sql` 會自動執行

---

## 🧪 測試應用程式

### 1. 測試首頁

訪問：http://localhost:8080

**預期結果：**
- ✅ 看到住宿列表
- ✅ 搜尋功能正常
- ✅ 排序功能正常

### 2. 測試登入

訪問：http://localhost:8080/login

**測試帳號：**
- 管理員：`admin` / `admin123`
- 業主：`owner1` / `owner123`
- 用戶：`user1` / `user123`

### 3. 測試 API

訪問：http://localhost:8080/swagger-ui/index.html

**預期結果：**
- ✅ 看到 Swagger UI
- ✅ 可以測試各個 API

### 4. 測試資料庫連接

在日誌中應該看到：
```
HikariPool-1 - Starting...
HikariPool-1 - Start completed.
```

### 5. 測試郵件（可選）

嘗試「忘記密碼」功能：
- 訪問：http://localhost:8080/user/forgot-password
- 輸入郵箱測試

---

## ❌ 常見問題

### 問題 1：應用程式無法啟動

**錯誤訊息：**
```
Error creating bean with name 'dataSource'
```

**原因：** 資料庫連接失敗

**解決方法：**
1. 確認 MySQL 正在運行
2. 確認資料庫 `booking_db` 存在
3. 確認密碼正確：`2FTA93108`

### 問題 2：端口被佔用

**錯誤訊息：**
```
Port 8080 is already in use
```

**解決方法：**
```cmd
# 查看佔用端口的進程
netstat -ano | findstr :8080

# 結束進程（替換 PID）
taskkill /F /PID <PID>
```

### 問題 3：資料表未創建

**原因：** DDL 模式設定問題

**解決方法：**
確認 `application.properties` 中：
```properties
spring.jpa.hibernate.ddl-auto=${DDL_AUTO}
```

啟動腳本已設定：`DDL_AUTO=create-drop`

### 問題 4：找不到 mvn 命令

**錯誤訊息：**
```
'mvn' is not recognized
```

**解決方法：**
1. 確認已安裝 Maven
2. 確認 Maven 在 PATH 中
3. 或使用 IDE（IntelliJ IDEA）運行

---

## 📊 啟動成功的標誌

當你看到以下訊息，代表啟動成功：

```
  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/
 :: Spring Boot ::                (v3.2.5)

2025-11-09 14:XX:XX.XXX  INFO 12345 --- [  restartedMain] c.e.booking.BookingApplication          : Starting BookingApplication using Java 17
2025-11-09 14:XX:XX.XXX  INFO 12345 --- [  restartedMain] c.e.booking.BookingApplication          : No active profile set, falling back to 1 default profile: "default"
...
2025-11-09 14:XX:XX.XXX  INFO 12345 --- [  restartedMain] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat started on port(s): 8080 (http)
2025-11-09 14:XX:XX.XXX  INFO 12345 --- [  restartedMain] c.e.booking.BookingApplication          : Started BookingApplication in 15.234 seconds
```

**關鍵訊息：**
- ✅ `Tomcat started on port(s): 8080`
- ✅ `Started BookingApplication in X seconds`

---

## 🎯 測試完成後

如果本地測試成功：

1. ✅ **確認環境變數配置正確**
   - 資料庫連接正常
   - 郵件設定正常

2. ✅ **準備部署到 Zeabur**
   - 在 Zeabur 設定相同的環境變數
   - 連接新的 GitHub 倉庫
   - 部署！

3. ✅ **未來開發**
   - 使用 `start-dev.bat` 快速啟動
   - 修改代碼後自動重載
   - 推送到 GitHub 自動部署到 Zeabur

---

## 📝 環境變數說明

`start-dev.bat` 設定的環境變數：

| 環境變數 | 值 | 說明 |
|---------|-----|------|
| `DATABASE_URL` | `jdbc:mysql://localhost:3306/booking_db?...` | 本地 MySQL |
| `DB_USERNAME` | `root` | MySQL 用戶名 |
| `DB_PASSWORD` | `2FTA93108` | MySQL 密碼 |
| `MAIL_USERNAME` | `howie960018@gmail.com` | Gmail 帳號 |
| `MAIL_PASSWORD` | `rgsqpqcanthwqars` | Gmail 應用程式密碼 |
| `APP_BASE_URL` | `http://localhost:8080` | 本地網址 |
| `DDL_AUTO` | `create-drop` | 開發模式（自動創建/刪除資料表） |
| `SHOW_SQL` | `true` | 顯示 SQL 語句（開發用） |

---

## 🚀 立即執行

**雙擊運行：**
```
C:\my-booking-app-practice\start-dev.bat
```

然後：
1. 等待啟動完成
2. 打開瀏覽器訪問 http://localhost:8080
3. 測試功能！

---

**需要幫助？** 查看上方的常見問題章節。

